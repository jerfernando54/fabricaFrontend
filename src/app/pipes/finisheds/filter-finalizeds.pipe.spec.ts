import { FilterFinalizedsPipe } from './filter-finalizeds.pipe';

describe('FilterFinalizedsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFinalizedsPipe();
    expect(pipe).toBeTruthy();
  });
});
